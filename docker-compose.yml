  services:
    catalogdb:
      image: postgres:16
    basketdb:
      image: postgres:16

    catalog-api:
      image: ${DOCKER_REGISTRY-}catalogapi
      build:
        context: .
        dockerfile: Catalog-API/Dockerfile

  volumes:
    postgres_catalog:
    postgres_basket:
